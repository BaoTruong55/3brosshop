@model LoginWithRecoveryCodeViewModel
@{
    ViewData["Title"] = "Xác minh mã khôi phục";
}
<main id="mainContent" class="main-content">
<div class="page-container ptb-60">
    <div class="container">
        <section class="sign-area panel p-40">
            <h2>@ViewData["Title"]</h2>
            <hr />
            <p>
                Bạn đã yêu cầu đăng nhập bằng mã khôi phục. Thông tin đăng nhập này sẽ không được ghi nhớ cho đến khi bạn cung cấp mã ứng dụng xác thực khi đăng nhập hoặc vô hiệu hóa 2FA và đăng nhập lại.
            </p>
            <div class="row">
                <div class="col-md-4">
                    <form method="post">
                        <div asp-validation-summary="All" class="text-danger"></div>
                        <div class="form-group">
                            <label asp-for="RecoveryCode"></label>
                            <input asp-for="RecoveryCode" class="form-control" autocomplete="off" />
                            <span asp-validation-for="RecoveryCode" class="text-danger"></span>
                        </div>
                        <button type="submit" class="btn btn-default">Đăng nhập</button>
                    </form>
                </div>
            </div>
        </section>
    </div>
</div>
</main>